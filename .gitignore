# Local-only directories and notes
.claude/
.vscode/
.git.backup/
backUpDirectives.md

# Node — only relevant if someone ever runs `npm i` (we have zero deps)
node_modules/
package-lock.json

# Logs
*.log

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Discord data exports — never commit these (they contain every DM you've ever had)
package/
messages/
export/
*.zip